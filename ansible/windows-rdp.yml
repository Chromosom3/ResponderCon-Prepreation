---
- name: Windows 10 Remote Desktop Setup
  hosts: windows10
  gather_facts: no
  tasks:
  - name: Adding Domain Users to RDP
    ansible.windows.win_command: Add-LocalGroupMember -Group "Remote Desktop Users" -Member "DOMAIN\Domain Users"